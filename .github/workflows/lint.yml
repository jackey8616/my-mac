name: Lint

on:
  push:
  pull_request:

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck
        # Scans all *.sh files in the repo (bootstrap.sh) by default.
        uses: ludeeus/action-shellcheck@master
